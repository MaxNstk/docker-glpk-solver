version: '3'
services:
  glpk:
    container_name: glpk-solver
    stdin_open: true
    build: 
      context: .
      dockerfile: ./Dockerfile
    command: python arquivos_professor/caminhos_minimos/transporte_binario.py 
    volumes:
      - .:/code
      - glpk-data:/data
volumes:
  glpk-data: